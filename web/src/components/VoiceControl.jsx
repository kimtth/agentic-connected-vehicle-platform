const VoiceControl = ({ vehicleId }) => {
    console.log('VoiceControl component loaded for vehicle:', vehicleId);

    return (
        <>
            <div>Under construction ... </div>
        </>
    )
};

export default VoiceControl;
