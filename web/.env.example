# ============================================================================
# Azure AD / Microsoft Entra ID Configuration
# ============================================================================
# Get these values from your Azure App Registration in the Azure Portal

# REQUIRED: SPA App Registration Client ID (GUID format)
# - Navigate to Azure Portal > App registrations > (Your SPA App) > Overview
# - Copy the "Application (client) ID"
# - This MUST be the client GUID, NOT the api:// Application ID URI
REACT_APP_AZURE_CLIENT_ID=00000000-0000-0000-0000-000000000000

# REQUIRED: Azure AD Tenant ID
# - Found in the same Overview page as "Directory (tenant) ID"
# - Or use "common" for multi-tenant, "organizations", or "consumers"
REACT_APP_AZURE_TENANT_ID=your-tenant-id

# OPTIONAL: API Scope for Backend Access
# - Navigate to Azure Portal > App registrations > (Your API App) > Expose an API
# - Copy the "Application ID URI" and append the scope name
# - Leave empty if not calling a protected backend API
REACT_APP_AZURE_SCOPE=api://<your-application-id>/access_as_user

# ============================================================================
# Backend API Configuration
# ============================================================================
# Backend API base URL for development
# - Change this to match your backend server's address
REACT_APP_API_DEV_BASE_URL=http://localhost:8000

# ============================================================================
# Development Configuration
# ============================================================================
# Node environment: development | production
# - Enables React developer tools and detailed error messages when set to "development"
NODE_ENV=development