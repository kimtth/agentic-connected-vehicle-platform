# Azure AD / Microsoft Entra ID Configuration
# Get these values from your Azure App Registration
# Frontend must use the SPA App Registration client GUID (not the api:// Application ID URI).
REACT_APP_AZURE_CLIENT_ID=<spa-client-guid>
REACT_APP_AZURE_TENANT_ID=yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy
REACT_APP_AZURE_SCOPE=api://<your-api-app-id-uri>/access_as_user

# Optional: Explicit redirect URI (defaults to current origin)
REACT_APP_AZURE_REDIRECT_URI=http://localhost:3000

# Backend API URL: Development
REACT_APP_API_DEV_BASE_URL=http://localhost:8000

# Set NODE_ENV for development tools: development | production
NODE_ENV=development
